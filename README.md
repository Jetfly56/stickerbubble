# StickerBubble

macOS bubble sticker sender (SwiftPM) plus a small **Railway** API in `railway-server/` (Node, Postgres).

- Run the app: `swift run` from this folder (macOS 13+).
- Deploy the API: see `railway-server/README.md`. In the app, use **Account & sync…** to paste the server URL and manage contacts.
